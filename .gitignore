# Raíz: solo pnpm (pnpm-lock.yaml). Evita locks duplicados si alguien usa npm/bun por error.
/package-lock.json
bun.lock

# build output
dist/

# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
.env
.env.production

# Log del instalador Windows (local)
install-bichipishi-log.txt

# API: Docker usa npm ci + package-lock.json (no pnpm en metrics-api)
metrics-api/pnpm-lock.yaml

# credenciales / ajustes locales de la API + SQLite de rendimiento (generado en runtime)
metrics-api/data/settings.json
metrics-api/data/perf.sqlite
metrics-api/data/perf.sqlite-shm
metrics-api/data/perf.sqlite-wal

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/
