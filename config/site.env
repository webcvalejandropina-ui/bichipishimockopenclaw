# Primer nombre = URL que funciona en todos los equipos sin tocar hosts (DNS nip.io → 127.0.0.1).
# Segundo = bichipishi.home (scripts/ensure-hosts.mjs lo añade a hosts cuando hay permiso).
BICHI_SITE_HOST=bichipishi.127.0.0.1.nip.io bichipishi.home
