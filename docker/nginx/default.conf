# Obsoleto en la imagen: la config real sale de docker/nginx/*.conf.template
# vía /docker-entrypoint.d/35-bichi-nginx.sh (variables BICHI_SITE_HOST, BICHI_NGINX_PROFILE).
# Referencia manual del modo “API en contenedor”: ver docker-api.conf.template
